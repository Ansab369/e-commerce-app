import 'package:flutter/material.dart';

class AccountScreeen extends StatelessWidget {
  const AccountScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profille Screen'),
      ),
    );
  }
}
